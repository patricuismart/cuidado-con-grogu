import '../styles/main.scss';

const Header = () => {
  return (
    <header className="header">
      <section className="section1">Reglas</section>
      <section className="section2">Fichas</section>
      <section className="section3">Juego</section>
    </header>
  );
};

export default Header;
